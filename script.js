function generateQuestion2(gameState) {
    const brotherAge = gameState.age + (gameState.year - 1872);
    // other logic...
}

function generateQuestion3(gameState) {
    const situationText = `The situation is critical for someone aged ${gameState.age + (gameState.year - 1872)}...`;
    // other logic...
}

function generateQuestion4(gameState) {
    const situationText = `At this year, the person is ${gameState.age + (gameState.year - 1872)} years old...`;
    // other logic...
}

function generateQuestion7(gameState) {
    const correctAge = gameState.age + (gameState.year - 1872);
    // other logic...
}

function calculateEnding(gameState) {
    const ageInDeathEnding = gameState.age + (gameState.year - 1872);
    const otherEndingAge = gameState.age + (gameState.year - 1872);
    // other logic...
}

// Additional code where age may be displayed or calculated incorrectly should be updated accordingly. 
